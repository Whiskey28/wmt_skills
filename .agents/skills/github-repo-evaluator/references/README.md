# references / 附录占位

本目录用于 **渐进披露**：当 `SKILL.md` 接近篇幅上限时，可将**评分细则扩展**、**各语言栈命令附录** 或 **API 字段说明** 拆到独立 `.md`，在 `SKILL.md` 中只保留「何时读哪一篇」的指针。

当前 **github-repo-evaluator** 主流程仍以根目录 `SKILL.md` 为唯一真源；此处文件可为空，仅作结构预留。
