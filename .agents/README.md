# .agents 目录说明

该目录通常用于放置**项目级 Agent Skills**（例如 `.agents/skills/*`）以及与 Agent 工作流相关的辅助文件（如 planning）。

> 注意：如果 `.agents/` 未纳入 git 版本控制（未 tracked），删除后无法通过 git 自动恢复。
> 建议明确：你希望 `.agents/` 在本仓库中是“可提交的资产”还是“本地环境镜像/缓存”。

